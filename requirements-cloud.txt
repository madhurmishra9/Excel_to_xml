# Optional cloud-storage backends for `fetch` (lazily imported).
# Install only the ones you need:  pip install -r requirements-cloud.txt
boto3>=1.20                  # s3://bucket/key
google-cloud-storage>=2.0   # gs://bucket/key
gdown>=4.0                   # gdrive://<id> or Google Drive share URLs (public files)
azure-storage-blob>=12.0    # az://container/blob or *.blob.core.windows.net URLs
